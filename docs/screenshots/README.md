# Screenshots for the report (PDF)

Add exported images here or embed them in your final document. Suggested captures:

1. **Azure Portal — Resource group**  
   Shows `diet-analysis-rg` (or your name) with Storage, Function App, Static Web App.

2. **Blob Storage**  
   Storage account → container → `All_Diets.csv` visible in the list.

3. **Function App — live endpoint**  
   Browser or Postman showing JSON from `https://<function-app>.azurewebsites.net/api/analyze` (blur secrets if any).

4. **Static Web App — dashboard**  
   Full page with charts, filters, and execution time visible.

5. **GitHub**  
   Repository home page showing `backend/`, `frontend/`, `docs/`, and recent commits.

Name files clearly, for example: `blob-container.png`, `function-json.png`, `dashboard.png`, `github-repo.png`.
